<% if current_user.avatar_public_id.present? %>
  <%= image_tag current_user.avatar_url, class: "w-10 h-10 rounded-full" %>
<% else %>
  <%= image_tag 'default_avatar.png', class: "w-10 h-10 rounded-full" %>
<% end %>