<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Upload Users</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 h-screen flex items-center justify-center">

<nav class="flex justify-center md:py-2 mx-4 bg-white w-full">
  <div class="flex w-full md:px-6 justify-between items-center">
    <div class="flex gap-4 p-3 items-center">
      <p class="text-2xl md:text-3xl font-serif font-semibold text-black-600">Upload Users (CSV/XLSX)</p>
    </div>
    <div class="flex gap-4 p-2 items-center">
      <img class="w-8 h-8 rounded-full" src="https://flowbite.com/docs/images/people/profile-picture-5.jpg" alt="user photo">
      <div class="flex flex-col">
        <h2 class="text-lg mb-1"><%= current_user.first_name %> <%= current_user.last_name %></h2>
        <div class="text-sm">Admin</div>
      </div>
    </div>
  </div>
</nav>
<hr class="w-full">

<div class="container mx-auto p-6 flex justify-center items-center h-full">
  <div class="bg-white p-6 rounded-lg shadow-md">
    <%= form_with url: upload_users_manage_users_path, multipart: true, method: :post, class: "space-y-4" do |f| %>
      <div>
        <label class="block font-semibold">Choose File</label>
        <%= f.file_field :file, required: true, accept: ".csv, .xlsx", class: "border p-2 rounded w-full focus:ring-2 focus:ring-blue-400" %>
      </div>
      
      <div>
        <%= f.submit "Upload", class: "bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600 cursor-pointer" %>
      </div>
    <% end %>
  </div>
</div>

</body>
</html>
