<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reports</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">

    <div id="main-content" class="transition-all duration-300">
        <!-- Navbar -->
        <nav class="flex justify-center md:py-2 mx-4 bg-white">
            <div class="flex w-full md:px-6 justify-between items-center">
                <div class="flex gap-4 p-3 items-center">
                    <p class="text-2xl md:text-3xl font-serif font-semibold text-gray-600">User Management</p>
                </div>
                <div class="flex gap-4 p-2 items-center">
                    <img class="w-8 h-8 rounded-full" src="https://flowbite.com/docs/images/people/profile-picture-5.jpg" alt="user photo">
                    <div class="flex flex-col">
                        <h2 class="text-lg mb-1"><%= current_user.first_name %> <%= current_user.last_name %></h2>
                        <div class="text-sm">Admin</div>
                    </div>
                </div>
            </div>
        </nav>
        <hr>

        <!-- Main Content -->
        <div class="flex items-center justify-center mt-[14%] ">
            <div class="w-full max-w-md">
                <!-- File Upload Section -->
                <div class="flex flex-col items-center justify-center w-full h-64 cursor-pointer bg-gray-50 hover:bg-gray-100 rounded-lg border-2 border-black">
                    <!-- Form -->
                    <%= form_with url: upload_users_manage_users_path, multipart: true, method: :post, local: true, class: "w-full px-4" do |f| %>
                        <div class="flex flex-col items-center space-y-4">
                            <!-- Upload Icon -->
                            <svg class="w-8 h-8 mb-4 text-gray-500" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 16">
                                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 13h3a3 3 0 0 0 0-6h-.025A5.56 5.56 0 0 0 16 6.5 5.5 5.5 0 0 0 5.207 5.021C5.137 5.017 5.071 5 5 5a4 4 0 0 0 0 8h2.167M10 15V6m0 0L8 8m2-2 2 2"/>
                            </svg>
                            <!-- File Input -->
                            <input type="file" name="upload_file" accept=".csv, .xlsx" class="border p-2 rounded w-full focus:ring-2 focus:ring-blue-400" required>
                            <!-- Upload Button -->
                            <%= f.submit "Upload", class: "bg-blue-500 text-white px-6 py-2 rounded-lg hover:bg-blue-600 cursor-pointer" %>
                        </div>
                    <% end %>
                    <!-- Descriptive Text -->
                    <p class="mt-4 text-sm text-gray-500"><span class="font-semibold">Click to upload</span> or drag and drop</p>
                    <p class="text-xs text-gray-500">CSV/XLSX</p>
                </div>
            </div>
        </div>

        <!-- Flash Messages -->
        <div id="flash-messages" class="text-center mb-4 mx-auto w-[90%]">
            <% if flash[:notice] %>
                <div class="bg-green-200 text-green-800 p-2 rounded"><%= flash[:notice] %></div>
            <% elsif flash[:alert] %>
                <div class="bg-red-200 text-red-800 p-2 rounded"><%= flash[:alert] %></div>
            <% end %>
        </div>
    </div>
</body>

</html>