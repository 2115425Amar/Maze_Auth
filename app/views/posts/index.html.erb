

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Maze</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-[#F8F9FD]">

<!-- NavBar -->
<nav class="flex justify-center md:py-2 mx-4">
  <div class="flex w-full md:px-6 justify-between items-center">
    <div class="flex gap-4 p-4 items-center md:w-1/3">
      <img src="<%= asset_path('Vector.svg')%>" class="w-8 md:w-10" alt="icon">
      <p class="text-2xl md:text-4xl font-serif font-semibold text-gray-800"><%= @title_text %></p>
    </div>

    <div class="flex gap-6 p-3 items-center md:w-2/3 md:border-2 border-[#3333334D] rounded-2xl">
      <img src="" class="w-6" alt="icon">
      <input class="hidden md:block w-full text-lg font-['Montserrat'] font-semibold text-gray-600 focus:outline-none" placeholder="Search for something here...">
    </div>
  </div>
</nav>

<div class="bg-gray-200 p-2 m-6 rounded-xl">
  <!-- Post Form -->
  <%= form_with(model: Post.new, url: posts_path, local: true) do |form| %>
    <div class="bg-white rounded-lg mt-8 mx-[2%] my-5">
      <div class="p-5 mx-6">
        <%= form.text_area :description, placeholder: "What's happening...?", class: "w-full px-4 py-3 bg-[#F5F6F8] rounded-xl focus:outline-none" %>
      </div>

      <div class="flex flex-col p-4 mx-6 border-t border-[#3333334D]">
        <span class="block text-base font-medium text-gray-900">Visibility</span>
        <div class="flex gap-4 pt-1 mt-2">
          <div class="flex items-center">
            <%= form.radio_button :public, "true", checked: true %>
            <span class="ml-2 text-gray-900">Public</span>
          </div>
          <div class="flex items-center">
            <%= form.radio_button :public, "false" %>
            <span class="ml-2 text-gray-900">Private</span>
          </div>
        </div>
      </div>

      <div class="flex justify-between p-4 mx-6 border-t border-[#3333334D]">
<!--        <div class="flex  text-gray-600">-->
<!--          <button class="flex items-center gap-2">-->
<!--            <img src="<%#=asset_path('dil.svg')%>" class="w-5" alt="Photo">-->
<!--            <img src="<%#= asset_path('dil.svg')%>" class="w-8 md:w-10" alt="icon">-->

<!--            <span class="hidden sm:inline">Photo/Video</span>-->
<!--          </button>-->
<!--        </div>-->

        <%# media icons %>
        <div class="flex justify-between p-4  border-[#3333334D] mx-6">
          <div class="flex gap-6 text-gray-600">
            <button class="flex items-center gap-2">
              <img src="<%= asset_path('dil.svg')%>" class="w-5" alt="Live Video">
              <span class="hidden sm:inline">Video</span>
            </button>
            <button class="flex items-center gap-2">
              <img src=<%=asset_path('dil.svg')%> class="w-5" alt="Photo/Video">
              <span class="hidden sm:inline">Photo/Video</span>
            </button>
            <button class="flex items-center gap-2">
              <img src=<%=asset_path('dil.svg')%> class="w-5" alt="Feeling">
              <span class="hidden sm:inline">Feeling</span>
            </button>
          </div>

      </div>
        <%= form.submit "Post", class: "px-12 py-3 bg-[#4378DB] text-white font-medium rounded-lg" %>

    </div>
  <% end %>
<!--<hr>-->
  <!-- Post Feed -->
  <div class="space-y-4">
    <% @posts.each do |post| %>
      <div class="mx-[2%] bg-white rounded-xl p-4 border border-[#3333334D]">
        <div class="flex justify-between items-start mb-3 mx-2">
          <div>
            <h3 class="text-lg mb-1"><%= post.user.first_name %> <%= post.user.last_name %></h3>
            <p class="text-sm text-[#666666]"> <%= time_ago_in_words(post.created_at) %> ago</p>
          </div>
        </div>

        <p class="text-[#666666] mb-4 mx-2"><%= post.description %></p>

        <div class="text-sm text-[#666666] mb-3 mx-2">
          <%= post.likes.count %> Likes • <%= post.comments.count %> Comments
        </div>

        <div class="flex gap-12 py-1 border-y border-[#3333334D] mx-2">
          <button class="text-gray-600">❤️ Like</button>
          <button class="text-gray-600">💬 Comment</button>
        </div>

        <div class="mt-4 mx-6 space-y-4">
          <h1 class="w-full font-medium text-gray-400 text-lg md:text-2xl">Comments</h1>
          <hr class="w-full border-gray-300">
          <% post.comments.each do |comment| %>
            <div class="flex justify-between items-center bg-gray-200 p-2 rounded">
              <span class="text-gray-800"><strong><%= comment.user.first_name %> <%= comment.user.last_name %>:</strong> <%= comment.content %></span>
              <span class="text-sm text-gray-500"><%= time_ago_in_words(comment.created_at) %> ago</span>
            </div>
          <% end %>
        </div>

        <%= form_with(model: [post, Comment.new], url: post_comments_path(post), local: true, class: "mt-3") do |form| %>
          <div class="flex space-x-2 p-2 bg-gray-100 rounded-lg">
            <%= form.text_field :content, placeholder: "Write a comment...", class: "flex-1 p-2 border rounded-lg focus:outline-none" %>
            <%= form.submit "Send", class: "px-3 py-2 bg-blue-500 text-white rounded-lg hover:bg-blue-600" %>

          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
</body>
</html>





